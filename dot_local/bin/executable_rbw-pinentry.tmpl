#!/usr/bin/env bash
set -euo pipefail

export GDK_BACKEND="${GDK_BACKEND:-wayland,x11}"
export GDK_SCALE="${PINENTRY_GDK_SCALE:-{{ if eq .hostname "fw12" }}1{{ else }}2{{ end }}}"

exec /usr/bin/pinentry-gtk "$@"
