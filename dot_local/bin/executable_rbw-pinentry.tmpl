#!/usr/bin/env bash
set -euo pipefail

export GDK_BACKEND="${GDK_BACKEND:-wayland,x11}"
export GDK_SCALE="${PINENTRY_GDK_SCALE:-{{ if ge .monitor_scale "1.5" }}2{{ else }}1{{ end }}}"

exec /usr/bin/pinentry-gtk "$@"
