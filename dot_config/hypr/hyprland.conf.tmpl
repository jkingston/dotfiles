# Monitor
{{ if eq .monitor_name "" -}}
monitor = , preferred, auto, {{ .monitor_scale }}
{{ else -}}
monitor = {{ .monitor_name }}, preferred, auto, {{ .monitor_scale }}
{{ end -}}

xwayland {
    force_zero_scaling = true
}

# Environment
env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24
env = GDK_BACKEND,wayland,x11,*
env = QT_QPA_PLATFORM,wayland;xcb
env = QT_AUTO_SCREEN_SCALE_FACTOR,1
env = ELECTRON_OZONE_PLATFORM_HINT,auto
{{- if eq .gpu "intel" }}
env = LIBVA_DRIVER_NAME,iHD
{{- end }}

# Input
input {
    kb_layout = gb
    touchpad {
        natural_scroll = true
        clickfinger_behavior = true
        tap-to-click = true
    }
}

gesture = 3, horizontal, workspace

gestures {
    workspace_swipe_forever = true
}

$mod = SUPER
$mod_shift = SUPER_SHIFT
$mod_ctrl = SUPER_CTRL
$mod_ctrl_shift = SUPER_CTRL_SHIFT
$mod_alt = SUPER_ALT

# Autostart
exec-once = xrdb -merge ~/.Xresources
exec-once = systemctl --user daemon-reload && systemctl --user enable --now hyprsunset-check.timer wallpaper-sync.timer wallpaper-rotate.timer
exec-once = uwsm app -- waybar
exec-once = uwsm app -- mako
exec-once = uwsm app -- swayosd-server
exec-once = wl-paste --watch cliphist store
exec-once = wl-paste --type image --watch cliphist store
{{- if not .is_vm }}
exec-once = hypridle
{{- end }}
exec-once = ~/.local/bin/hyprsunset-apply
exec-once = swww-daemon && ~/.local/bin/wallpaper-random

# App launchers
bind = $mod, RETURN, exec, uwsm app -- ghostty
bind = $mod, SPACE, exec, uwsm app -- rofi -show drun
bind = $mod_shift, B, exec, uwsm app -- librewolf
bind = $mod_shift, N, exec, uwsm app -- ghostty -e nvim
bind = $mod_shift, T, exec, uwsm app -- ghostty -e btop
bind = $mod_shift, F, exec, uwsm app -- nautilus
bind = $mod_shift, D, exec, uwsm app -- ghostty -e lazydocker
bind = $mod_shift, W, exec, uwsm app -- ghostty --class=com.floating.tui -e impala
bind = $mod_ctrl, S, exec, uwsm app -- localsend

# Window management
bind = $mod, W, killactive,
bind = CTRL ALT, DELETE, exec, hyprctl dispatch closewindow address:*
bind = $mod, T, togglefloating,
bind = $mod, O, pin,
bind = $mod, F, fullscreen, 0
bind = $mod_alt, F, fullscreen, 1
bind = $mod, G, togglegroup,
bind = $mod_alt, G, moveoutofgroup,
bind = $mod_alt, TAB, changegroupactive,

# Focus
bind = $mod, LEFT, movefocus, l
bind = $mod, RIGHT, movefocus, r
bind = $mod, UP, movefocus, u
bind = $mod, DOWN, movefocus, d
bind = $mod, H, movefocus, l
bind = $mod, L, movefocus, r
bind = $mod, K, movefocus, u
bind = $mod, J, movefocus, d

# Swap
bind = $mod_shift, LEFT, swapwindow, l
bind = $mod_shift, RIGHT, swapwindow, r
bind = $mod_shift, UP, swapwindow, u
bind = $mod_shift, DOWN, swapwindow, d
bind = $mod_shift, H, swapwindow, l
bind = $mod_shift, L, swapwindow, r
bind = $mod_shift, K, swapwindow, u
bind = $mod_shift, J, swapwindow, d

# Resize
bind = $mod, EQUAL, resizeactive, -100 0
bind = $mod, MINUS, resizeactive, 100 0
bind = $mod_shift, EQUAL, resizeactive, 0 100
bind = $mod_shift, MINUS, resizeactive, 0 -100

# Workspaces
bind = $mod, 1, workspace, 1
bind = $mod, 2, workspace, 2
bind = $mod, 3, workspace, 3
bind = $mod, 4, workspace, 4
bind = $mod, 5, workspace, 5
bind = $mod, 6, workspace, 6
bind = $mod, 7, workspace, 7
bind = $mod, 8, workspace, 8
bind = $mod, 9, workspace, 9
bind = $mod, 0, workspace, 10

bind = $mod_shift, 1, movetoworkspace, 1
bind = $mod_shift, 2, movetoworkspace, 2
bind = $mod_shift, 3, movetoworkspace, 3
bind = $mod_shift, 4, movetoworkspace, 4
bind = $mod_shift, 5, movetoworkspace, 5
bind = $mod_shift, 6, movetoworkspace, 6
bind = $mod_shift, 7, movetoworkspace, 7
bind = $mod_shift, 8, movetoworkspace, 8
bind = $mod_shift, 9, movetoworkspace, 9
bind = $mod_shift, 0, movetoworkspace, 10

bind = $mod, TAB, workspace, e+1
bind = $mod_shift, TAB, workspace, e-1
bind = $mod_ctrl, TAB, workspace, previous

bind = $mod, S, togglespecialworkspace, magic
bind = $mod_alt, S, movetoworkspace, special:magic

# Screenshots & tools
bind = , Print, exec, grimblast edit area
bind = SHIFT, Print, exec, grimblast copy screen
bind = $mod, Print, exec, hyprpicker -a

# Clipboard
bind = $mod, C, exec, wl-copy
bind = $mod, V, exec, wl-paste
bind = $mod_ctrl, V, exec, ~/.local/bin/rofi-clipboard

# Utilities
bind = $mod_ctrl, I, exec, hyprlock
bind = $mod_shift, SPACE, exec, pkill -SIGUSR1 waybar
bind = $mod_shift, O, exec, hyprctl --batch 'dispatch setprop active opaque toggle; dispatch setprop active noblur toggle'
bind = $mod_ctrl, N, exec, makoctl dismiss
bind = $mod_ctrl_shift, N, exec, makoctl dismiss -a
bind = $mod_ctrl, E, exec, uwsm app -- rofimoji
bind = $mod_ctrl, W, exec, uwsm app -- waypaper
bind = $mod_alt, W, exec, ~/.local/bin/wallpaper-random
bind = $mod, slash, exec, ~/.local/bin/keybind-help
bind = $mod, ESCAPE, exec, uwsm app -- ~/.local/bin/hypr-power-menu

# Mouse
bind = $mod, MOUSE_DOWN, workspace, e+1
bind = $mod, MOUSE_UP, workspace, e-1
bindm = $mod, mouse:272, movewindow
bindm = $mod, mouse:273, resizewindow

# Media keys
bindel = , XF86AudioRaiseVolume, exec, swayosd-client --output-volume raise
bindel = , XF86AudioLowerVolume, exec, swayosd-client --output-volume lower
bindel = , XF86AudioMute, exec, swayosd-client --output-volume mute-toggle
bindel = , XF86AudioMicMute, exec, swayosd-client --input-volume mute-toggle
bindel = , XF86MonBrightnessUp, exec, swayosd-client --brightness raise
bindel = , XF86MonBrightnessDown, exec, swayosd-client --brightness lower
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPrev, exec, playerctl previous
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPause, exec, playerctl play-pause

{{- if .is_laptop }}
# Lid switch
bindl = , switch:on:Lid Switch, exec, loginctl lock-session
bindl = , switch:off:Lid Switch, exec, hyprctl dispatch dpms on
{{- end }}

# Appearance
general {
    gaps_in = {{ .gaps_inner }}
    gaps_out = {{ .gaps_outer }}
    border_size = {{ .border_size }}
}

decoration {
    rounding = 0
    active_opacity = 0.95
    inactive_opacity = 0.88
    fullscreen_opacity = 1.0

    blur {
        enabled = true
        size = 6
        passes = 4
        ignore_opacity = true
        xray = false
        special = true
        new_optimizations = true
        noise = 0.02
        contrast = 0.9
        brightness = 0.8
        popups = true
        popups_ignorealpha = 0.6
    }

    shadow {
        enabled = true
        range = 8
        render_power = 3
    }
}

animations {
    enabled = true
    bezier = easeOut, 0.16, 1, 0.3, 1
    bezier = easeIn, 0.3, 0, 0.8, 0.15
    bezier = standard, 0.2, 0, 0, 1
    bezier = linear, 0, 0, 1, 1
    animation = windows, 1, 3, standard, popin 96%
    animation = windowsIn, 1, 3, easeOut, popin 96%
    animation = windowsOut, 1, 2, easeIn, popin 98%
    animation = windowsMove, 1, 2, standard
    animation = border, 1, 1, linear
    animation = fade, 1, 3, easeOut
    animation = workspaces, 1, 4, standard, slidefade 12%
}

misc {
    focus_on_activate = true
    font_family = Caskaydia Cove Nerd Font
}

# Window rules
windowrule = float on, match:class ^(com\.floating\.tui)$
windowrule = center on, match:class ^(com\.floating\.tui)$
windowrule = size 800 600, match:class ^(com\.floating\.tui)$
windowrule = float on, match:initial_class ^(com\.floating\.tui)$
windowrule = center on, match:initial_class ^(com\.floating\.tui)$
windowrule = size 800 600, match:initial_class ^(com\.floating\.tui)$
windowrule = float on, match:class ^(waypaper)$
windowrule = center on, match:class ^(waypaper)$
windowrule = size 900 700, match:class ^(waypaper)$
windowrule = opacity 1.0 override 1.0 override, match:class ^(mpv)$
windowrule = opacity 1.0 override 1.0 override, match:class ^(vlc)$
windowrule = opacity 1.0 override 1.0 override, match:class ^(com.obsproject.Studio)$
windowrule = opacity 1.0 override 1.0 override, match:class ^(zoom)$
windowrule = opacity 1.0 override 1.0 override, match:class ^(steam_app_.*)$
windowrule = opacity 1.0 override 1.0 override, match:fullscreen 1
windowrule = opacity 0.92 0.85, match:class ^(ghostty)$
windowrule = opacity 0.95 0.9, match:class ^(.*Nautilus)$

layerrule = blur on, match:namespace ^rofi$
layerrule = blur on, match:namespace ^waybar$
layerrule = ignore_alpha 0, match:namespace ^rofi$
layerrule = ignore_alpha 0, match:namespace ^waybar$
